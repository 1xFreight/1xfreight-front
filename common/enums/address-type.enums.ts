export enum AddressTypeEnum {
  PICK = "pickup",
  DROP = "drop",
}

export enum AddressExtraTypeEnum {
  PORT_LOADING = "port_loading",
  AIRPORT_LOADING = "airport_loading",
  PORT_DISCHARGE = "port_discharge",
  AIRPORT_DISCHARGE = "airport_discharge",
}

export enum AddressArrivalStatusEnum {
  ON_TIME = "on_time",
  LATE = "late",
}
